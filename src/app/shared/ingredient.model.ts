export class Ingredient {
    
    //TS allows you to add public right in constructor to declare variables here
    //same effect as the recipe creating variables and instantiating
    constructor(public name:string, public amount:number) {}
}